# 2026-08-18

## 水墨·无争主题生理学优化改造（v2.6 完成）

对 `themes/shuimo-wuzheng-theme.json` 完成 13 项色值替换（共 372 处），核心变化：
- 宣纸底 `#F6F5E1` → `#F5F1DE`（色相 56°→48°，烛光色温）
- 浓墨正文 `#2A2C30` → `#383A3D`（对比度 12.7→10.1:1，进入 7~10:1 舒适区）
- 焦墨 `#232527` → `#2C2E31`（12.0:1，保持层级）
- 朱砂 `#9C4A2F` → `#8B402F`（饱和 54%→49%，L 锥负荷降低）
- 远水蓝 `#3D6B8C` → `#3A6680`（微偏青灰，远离 S 锥峰值）
- 灰青注释 `#5F7178` → `#566971`（4.4→5.1:1，达到 WCAG AA）
- 淡墨 `#4A5A63` → `#475663`；水蓝 hover `#356B8C` → `#325F78`
- 选择区降饱和：`#3D6B8C40` → `#3A668038`（9 处）
- UI 背景联动：侧边栏 `#F0ECDA`、活动栏 `#EAE5CC`、行高亮 `#E8E2CA`、终端 `#F3F0DC`

**保持不变的色**：潭水青 `#1F6E5C`、赭石 `#80622E`、紫毫 `#6F5E8A`、松绿 `#3C785C`、清墨 `#5A5F66`、浮层 `#F8F6E9`。

## 测试套件修复（scripts/test_theme.py）

- 原引用不存在的 `shuimo-shijie-theme.json`（旧名"水墨·世界"），已改为 wuzheng/danqing/zhuyun 三主题
- `test_theme_deep` 参数化 `editor_bg_expected`/`check_unity`：无争 `#F5F1DE`（不做无界断言），丹青 `#F6F5E1`（无界）
- 注释/正文可分辨性阈值 2.3→1.9（正文 10:1 与注释 AA 在数学上互斥于 2.3，上限约 2.2）
- 水色 hue 窗口 120~260 → 100~300（覆盖松绿 115°、紫毫 263°/278°）
- CLI：`--shijie` → `--wuzheng`/`--danqing`/`--zhuyun`

**测试结果：87 通过 / 0 失败 / 4 WARN（竹韵暗色主题缺扩展键，正常）**

## README 更新

- 新增「水墨·无争」章节：v2.6 色板表（11 角色）+ 生理学设计要点
- 「水墨·世界」→「水墨·丹青」，色板表按实测值修正（注释松绿 `#4C7548`、潭青 `#1F7A85`、朱砂 `#9A3B26`、远水蓝 `#315D8C`、赭石 `#8A4B2F`、赭沙 `#6F6040`、紫毫 `#7A5B8C`）

## 丹青主题优化方案（评审，未实施）

对 `shuimo-danqing-theme.json` 提出优化方案（用户暂未确认实施）：
- 对齐无争 v2.6 的生理学标准（正文对比度降到 ~10:1 舒适区）
- 色板去重（#846616 vs #8A6D1F 等近重复色）、注释松绿与 git added 绿色相冲突、focusBorder 朱砂红与错误语义冲突
- 终端 bright 色与 normal 几乎无区分、list.warning 与 editorWarning 色不一致
- 缺失键：statusBarItem.remote*、editorUnicodeHighlight.background、terminalCommandDecoration.* 等

## 丹青 v2.7 改造完成（用户授权放手一搏，已实施）

`themes/shuimo-danqing-theme.json` 283 处改动，测试 87 通过 / 0 失败：
- 全局角色替换：浓墨 `#2A2C30→#383A3D`(12.8→10.4:1)、焦墨 `#232527→#2C2E31`、注释 `#4C7548→#51686E` 灰青(4.8→5.4:1)
- 语义解耦：focusBorder 朱砂→远水蓝 `#315D8C`；list.warning 统一藤黄；注释绿与 git added 绿分离（松绿专司新增/成功）
- 粗体层级：keyword 全系加粗对齐 semantic；storage.type/storage.modifier 去粗体；Go/C/Shell 删 storage.type 覆盖规则
- 终端 bright 梯度：绿 `#55834E`/黄 `#97751F`/青 `#26828E`；minimapSlider 旧墨残留修复
- 补齐 13 项缺失键：editorUnicodeHighlight.background `#84661626`、statusBarItem.remote*、terminalCommandDecoration.*、debugIcon.*、editorStickyScroll.shadow、panelTitle.activeBorder `#9A3B26`、editorSuggestWidget.selectedForeground、editorLineNumber.dimmedForeground 等
- 符号图标与代码 token 对齐（boolean/null/enumMember→#846616）
- README 丹青章节升级 v2.7 体例（对齐无争 v2.6），package.json 版本 2.6.1→2.7.0
- 对比预览：`scratch/danqing-v2.7-preview.html`（生成器 scratch/make_danqing_preview.py，scope 最长匹配渲染）

## 待办

- Phase 6 真机验证：在 M1 上打开多语言文件，连续阅读 30 分钟体感评估
- 如已安装旧版主题，需重新打包发布（`npm run build`）
